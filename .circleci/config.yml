version: 2
jobs:
    build:
        working_directory: /work
        docker:
            - image: docker:18.02.0-ce-git
        steps:
            - run: echo "build"
    push:
        working_directory: /work
        docker:
            - image: docker:18.02.0-ce-git
        steps:
            - echo "push"
workflow:
    version: 2
    docker-build:
        jobs:
            - build
            - push:
                requires:
                    - build
